services:
  - type: web
    name: mdl-ynor-api
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/corpus/status
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
